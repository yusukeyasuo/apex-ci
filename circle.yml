machine:
  python:
    version: 2.7.10

dependencies:
  pre:
    - curl https://raw.githubusercontent.com/apex/apex/master/install.sh | sh
test:
  override:
    - echo 'test'

deployment:
  master:
    branch: master
    commands:
      environment:
      - apex deploy --env stg
